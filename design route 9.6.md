> **NPC 映射状态更新（2026-09-20）**：本文保留早期分析。Hsinlung 后续已确定造型与映射，现行规格见 [NPC 映射规格](docs/design-plans/npc-skeleton-mapping.md)，供给流程见 [动作供给路线](design_route_npc_motion_supply.md)。本文中的直接 77→11 抽点、旧比例/肩部方案、线条或体积待选等表述不再作为当前实现依据；游戏集成仍未完成。

> **status: snapshot**
> **过期提示**：§5 与 §7 所说的“Kimodo 未在本机运行过”已过时，2026-09-19 本机试用单人动作可用，动作供给方案以 design_route_npc_motion_supply.md 为准；§8 的“下一步未定”见 docs/roadmap.md。

# 路线结论 2026-09-06

状态：**讨论结论记录，不是执行方案。** `tasks.md` 未改动，无任何开工许可。
基线：`claude/velocity-unification-v1-946h9l` @ `07aa9f0`
分工：Claude 分析，Hsinlung 决策。本文档区分"已拍板"与"待定"，不要混读。
关联：`design_motion_supply.md`（v2）、`sth/asset-import-spike/PIPELINE.md`、`action_inventory.md`

---

## 0. 这轮改变了什么

三条判断同时翻转，而且互相支撑：

1. **环境**：从"三渲二"（3D 出图、运行时 2D）转向**实时 3D** 成为主候选。
2. **NPC**：关节数据**升三维**从 v2 的"可选优化"恢复为**前提**。
3. **动作来源**：外部供给从"动捕库里检索"变成"**文字生成**"，v2 §4 的盘子已过期。

第 3 条是第 2 条的理由，第 1 条和第 2 条互为条件。

---

## 1. Hsinlung 本轮明确的表态

拍板的：

- **可以重写引擎。** `js/` 的行为 / 新闻 / 目击 / 记忆变异系统接受重写。
- 画面最不满意的两条是**素材简陋**和**空间不可信**（不是"人太小看不清"）。
- 一个大场景预期 **30 栋以上不重样建筑**（不只做一条街，要居民楼、特殊建筑等）。
- **核心诉求是复杂系统 + 丰富动作。** 动作数量不可妥协——"只做 30 条"这个项目就不成立。
- **允许大范围删改**，沉没成本不构成约束。

倾向但未最终拍板：

- "数据 3D、外观仍是线条"这个默认，反应是"还不错吧"。
- 3D 火柴人（有体积、有影子）能否接受：**不确定**，需要看到实物。

---

## 2. 三渲二为什么被否

**它不是 2D 和 3D 的折中，是 3D 的超集。**

制作端一分不省：照样找模型、清理、组装、打光、定相机。在这之上还要额外建一整套只为烘图存在的东西——烘焙器、fill/line/shadow 三层输出、透明裁边、锚点元数据、图集打包、`art-manifest.json`、`render-style.json`、Pixi 混合显示层。`PIPELINE.md` 的"制作工具"六条，实时 3D 一条都不需要。

它唯一买到的是"运行时是 2D"，要付的是**每个变化维度都变成一次重烘**：朝向 × Look × 时段 × 远近 LOD。滤镜参数还在调，每调一次已烘的全部作废。

还有一条更直接的：**这个画风本来就是实时的。** moebius shader、深度描边、色阶量化、方向光阴影都是 Godot 里逐帧算的。烘图不是在生成画风，是**给实时效果拍照，然后丢掉深度、法线和分层**。三渲二在历史上成立是因为出图后要手工修补笔触，本项目不修图，理由不成立。

三渲二唯一的真优点：**在 2D 世界里火柴人不需要解释自己是什么**——同一平面、无体积、无穿透、无"影子从哪来"。Hsinlung 接受重写引擎之后，这个优点的权重下降。

**没有沉没成本**：图集 / 锚点 / manifest / Pixi 混合层这一整块尚未开工。

**保留有效的部分**（与烘图无关，实时 3D 一样用）：221 个 GLB 工作库、Blender 封装与 `convert_gltf_to_glb.py`、按需清理规则与 `blacken_fake_interiors.py`、许可证记录、Godot 街角场景与可切换滤镜、`PIPELINE.md` 里的组合样片验收清单、米制/原点/朝向的模型规范。

---

## 3. NPC：数据 3D，外观仍是线条

**必须区分两件事，它们一直被混着说：**

| | 含义 | 本轮结论 |
|---|---|---|
| 数据 3D | 11 个关节各有 xyz，绘制时投影连线 | **前提** |
| 外观 3D | 有体积、网格、被光照、投影子（圆柱胶囊小人） | **未决，可永远不做** |

数据 3D 的实际改动很小：现在是 11 个点各有 xy 直接连线，以后是 11 个点各有 xyz、投影一步再连线。**渲染出来仍是等宽线条，没有体积、网格、光照。** 转身 = 画之前绕竖轴转一下，任意角度，零新增资产。

投影规则不用新发明：`Projection.js` 那套原样施加到关节的 z 上即可（v1 §3 已写清）。

**保留**：67 条 clip（可半自动升维，见下）、§5.5 末端目标点 + 余弦定理（两骨 IK 本来就是三维数学，二维才是特例）、`action_inventory.md`、行为/新闻逻辑、stick-puppet（需加 z 轴）。

**作废**：Pixi 渲染层、`drawObliqueBox` 手工三面体、`ClipLibrary.js:191` 的 `v.length !== 2` 卡口。

### 3.1 clip 升维的依据与风险

v2 §1.2 量到的事实在这里变成资产：**深度已经编码在骨长压缩里**（`use_trash` 前臂 25 → 12.65，x 几乎没动）。骨长已知、屏幕 dx/dy 已知，则

```
z = ±√(骨长² − dx² − dy²)
```

正负号取作者原来的弯曲方向。**无参数可调。**

**风险**：那些压缩是"为了侧视好看"画的，不一定对应真实三维姿势，转过去可能散。

**证伪办法**（纯算术 + 一张对比图，不碰引擎、不开 Godot、不需要任何决定）：把 `walk`（侧视 20 帧）按上式升维，转 90° 与现有 `walk_front` 比。看三个数——根号内为负的帧数（骨头被画长，无解）、正负号翻面帧数、转换后与 `walk_front` 的关节平均差。对得上说明是同一三维姿势的两个投影；对不上说明 front 和 side 是各画各的，能自动升维的比例要打折。

---

## 4. 撤销 v2 §4.3 的降级

v2 把"骨架升三维"从 v1 的核心分叉降为"可以随时做也可以永远不做的优化"，理由是"三维才能做 IK"被推翻（余弦定理二维就能做）。

**该论证本身正确，但它只检查了 IK 这一条理由。朝向连续这条没被检查，而那才是三维真正的必要性所在。**

在当前 2D 斜视角世界里朝向只有三档还能忍（`Motor.js:336` 甚至不分正背，走开的人是脸朝你的）；一旦地面是真 3D，这条立刻变成主要缺陷。而生成模型的输出本来就是三维的，压扁成单朝向等于自己造回"每个方向一份资产"的老问题。

**结论：§4.3 的降级撤销，恢复为前提。理由是朝向，不是 IK。**

---

## 5. 动作供给：外部来源的盘子变了

v2 §4.1 按"Mixamo 约 2500 条、偏战斗舞蹈、日常覆盖不了"算账。该结论对 Mixamo 仍然成立，**但它不再是终点**。

**NVIDIA Kimodo**（2026-03 开源，代码 Apache-2.0，权重走 NVIDIA Open Model 许可，训练数据 700 小时商用许可动捕）：文字描述 → 3D 人体动作的扩散模型。

对上本项目的点：

- **输出就是关节坐标**（`posed_joints` 每帧每关节三维位置），不是蒙皮模型。从其骨架挑出 11 个关节是**挑不是解**，v1 §4.1 的映射表逻辑照用。
- **每帧带脚接触标记**（左右脚跟脚尖）——正好对应"cycle clip 必须 `abs_y ≤ 0`"的贴地门。
- 除文字外，还支持**末端执行器位置/旋转、2D 路径、关键帧姿势**作为约束——与 §5.5 的末端目标点是同一概念，接口现成。
- 显存 ~17GB；文字编码器放 CPU 可降到 3GB 以下，4060 Laptop 可跑。

**所以"上百条日常社会动作没有平替"这句作废**——不是去库里找，是描述一句生成一条。

### 5.1 仍不覆盖的

- **双人持续接触**（清单类 4：搂肩、扶人、架着走、抱小孩）——生成模型是单人的。仍是手工双人摆，`participants` 格式已存在。
- **道具对接**（清单类 3：手必须落在门把上）——仍靠 IK + 挂点。`scene.json` 缺挂点声明这一条没变（v2 §6.3）。

### 5.2 新出现的主风险

**真实动作在 47.6px 上可能读不出来。**

手画的 67 条不是真实的，是**夸张**过的——`use_trash` 前臂压到 51%，为的是在 47px 高、3px 宽的线条人身上让人看出"往前扔了个东西"。真人挠头幅度很小；降到 11 关节、无脸无手指、47px 高，很可能什么都看不出来。

清单里"掸衣服 / 嗑瓜子 / 整理头发 / 听耳机头微晃"这一批全是这种小幅动作。

**瓶颈可能从"动作从哪来"变成"真实动作怎么放大成能读的动作"。** 这个问题比前一个小，且可试：生成一条"挠头"，按骨架降维，放到 47px 看一眼。

---

## 6. 未决项

1. **影子。** 环境实时 3D 有真影子，人若只是线条就没影子——一屋子有影子的东西里站着几个没影子的人。三条出路：环境也不开影子 / 给人一个不绘制的几何代理专供投影 / 接受人没影子。只能看图，但它决定 Godot 场景灯光要不要开。
2. **47px 可读性**（§5.2）。当前最大未知。
3. **双人接触与道具对接的供给**（§5.1）。不是新问题，但不会被这次的新东西解决。
4. **引擎迁移工作量未量化。** "可以重写"和"知道有多少"是两回事。行为/新闻/目击/记忆变异是纯逻辑可一比一翻译；渲染、投影、相机、UI 全废。数字没数过。
5. **引擎选择未定**：Godot（要换语言）vs 浏览器内 3D（保住 `js/`）。本轮未展开。
6. **素材质量不会因为换实时 3D 而自动改善。** "空间不可信"会自动解决（尺寸就是尺寸），"素材简陋"不会——Quaternius 那批模型还是那批模型。Godot 街角样片对新方案一样有效。
7. **外观 3D（体积火柴人）未决**，且可永远不做。若最终要做，67 条 clip 与 stick-puppet 的存留结论完全不同。
8. v2 §9 的旧未决项原样有效：末端点超出两骨之和的政策（拉长 vs 钳回）、变异系数取值范围、C 类会合层（DuetStager 至今未实现，靠 `REACH_SLACK=2.5` 顶着）、挂点声明格式、道具供给（22 个 draw 文件 2295 行的另一半问题）。

---

## 7. 本文档没有验证的

**一张实际图都没有。** 尤其：

- 火柴人放进 Godot 街角场景是什么样（这件事在前几轮被反复提出，始终未做）
- clip 升维后转 90° 的效果（§3.1 的测试未跑）
- 生成动作降到 11 关节 47px 的可读性（§5.2 的测试未跑）

Kimodo 的能力描述来自其公开文档，**未在本机安装或运行过**。

---

## 8. 下一步

未定。Hsinlung 暂停本线，后续继续的方向待定。§3.1 和 §5.2 两个测试都不需要做任何选择即可执行，是成本最低的两个入口。