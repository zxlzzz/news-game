> **status: finalized**
> **过期提示（2026-09-19）**：本机已试用 Kimodo，单人动作可用；文首及 §8 关于本机尚未运行、能否运行的说法已过时。最终骨架的关节数尚未确定，§1/§2 的“11 个关节”不是已定规格；降到最终骨架后的动作可读性尚未验证。

# NPC 动作素材供给 — 设计路线

2026-09-16 讨论结论记录。本文只描述素材从哪来、怎么进管线，不含实现细节。

**Kimodo 未在本机安装或运行过。** 下文关于它的能力全部来自官方 README 与文档，未实测。

关联：
- `design_route_npc_behavior.md` — 行为系统，决定播哪个 json
- 09-06 路线结论 — 三渲二为什么否、关节为什么升三维、文字生成怎么进来的

---

## 0. 这个文档解决的问题

**大量动作 json 从哪来，而且不靠逐个手摆关节。**

行为系统的最小单元是一个动作 json，动作数量不可妥协。本文只管供给。

不管：播哪个 json（行为系统）；火柴人画成线条还是体积（§6）；引擎。

---

## 1. 前提（09-06 定下的方向）

- json 存 11 个关节每帧的**三维坐标**。
- 朝向不做成资产：绘制时绕竖轴转到当前朝向，再投影。一条 json 覆盖所有朝向。
- 所以"线条火柴人还是体积火柴人"只是渲染器怎么画，和素材供给无关。

---

## 2. 管线

```
一句描述（+ 可选约束）
  → Kimodo 生成：77 个关节每帧的三维坐标 + 左右脚跟脚尖触地标记
  → 从 77 个里挑出 11 个
  → 骨长对齐到我们的骨架
  → 动作 json
```

- 挑关节是挑，不是解。
- **骨长对齐怎么做未定。** 生成骨架的比例和我们的不同。这和旧文档里"末端点超出两骨之和时拉长还是钳回"是同一个问题。
- 每条描述生成上限 10 秒，对一个动作 json 够用。更长的拆成几段，Kimodo 自带多段衔接。

---

## 3. 三类动作各从哪来

| 类别 | 例子 | 来源 | 状态 |
|---|---|---|---|
| 单人 | 挠头、刷手机、扔垃圾、老人走 | 直接生成；风格写进描述（"一个老人……"） | 在官方覆盖范围内：走跑、手势、日常活动、常见物件交互、疲惫/醉酒/年老等风格 |
| 贴合物件 | 坐长椅、开门、在摊位前挑东西 | 生成时给手/脚末端位置约束，剩下的误差用端点 IK 修 | 官方报告末端位置误差约 3.6cm，未实测 |
| 双人接触 | 握手、搀扶、背人、搂肩 | 先生成 A；再生成 B，把 B 的手在接触那几帧约束到 A 身上 | **推断，未验证。** 单向：A 不会对 B 做出反应 |

双人的备选是专门的双人生成模型或数据集（见附录）。现在能直接跑的少，数据集多为科研限定。

---

## 4. 生成之后怎么改

生成出来允许改，不要求一次生成对。改有两层，都不是逐帧摆关节：

1. **改约束重新生成**（主要改法）：在 Kimodo 界面的时间轴上挪几个关键帧的手脚落点或全身姿势，模型把中间补齐。
2. **转成端点格式手修**（只修最后的误差）：每帧约 12 个数，就是四个肢体端点加弯曲方向，用已定的"端点定位 + 余弦定理"重建。

---

## 5. 和行为系统的接口

**行为系统不要求素材具有统一的首尾姿势或世界坐标。** 位置接续由播放端负责，见 `design_route_npc_behavior.md` §5：切换时以 NPC 当前所在位置接续下一条片段，消费片段内根节点的相对位移，不直接套用素材的绝对坐标。移动片段与导航的协调属于播放/移动实现。

行为可在任意帧被替换，不保证播完；因此首尾钉姿势不是切换连续性的主要机制。现有素材不因没有统一首尾而作废，不据此批量重出，也不强制增加分类字段或分类文件夹。一次性、持续状态、移动是使用方式，由行为侧决定。

首尾约束、循环加工、进入/保持/退出分段都是按实际需要选用的加工方式。需要三段时可以在一条完整素材上记分段位置，不必生成三份。首尾姿势相同不保证循环自然；同一只脚连续两次落地只能帮助定位步态周期，不能保证全身和朝向在接缝处连续。移动素材也不应一律钉回同一世界坐标。

当前设计不要求切换前先播退出段。姿势过渡是否需要补，等游戏中的实际效果再决定。

---

## 6. 明确不在这里

| | 去处 |
|---|---|
| 外观：线条火柴人 vs 体积火柴人 | 同一份数据，只是渲染器怎么画，要看实物决定。体积版能投出影子（09-06 未决项第 1 条） |
| 引擎、影子、环境素材 | 09-06 路线结论 |
| 现有 67 条手画 clip 升三维 | 09-06 §3.1，和本文并行 |
| 播哪个 json | `design_route_npc_behavior.md` |

---

## 7. 许可与来源记录

- 现阶段定位：科研 / 游戏雏形，非商用。
- **Kimodo**：代码 Apache-2.0。SOMA 系列模型用 NVIDIA Open Model 许可；SMPL-X 版本是 R&D 许可 → 用 SOMA 系列。文字编码器依赖 `meta-llama/Meta-Llama-3-8B-Instruct`，要在 Hugging Face 上申请访问权限。
- **InterHuman**（双人数据集）：只许用于科学研究。
- 提议（未拍板）：每条 json 记一个来源字段，写明由哪个模型或数据集生成。将来要发布时，能按来源把受限的整批剔除。

---

## 8. 未验证 / 风险

1. 真实动作放到火柴人上读不读得出来（09-06 §5.2）。摄像机缩放能减轻，减轻多少要看图。
2. 末端约束在我们的场景尺度下实际准不准。
3. 双人"生成两次"拼出来像不像。
4. 骨长对齐（§2）。
5. 本机（4060 Laptop）跑不跑得动，一条要多久。

---

## 附：参考过的现成东西

不直接采用。机制细节不要信这份文档，要用就重查原文。

- **Kimodo**（NVIDIA，2026-03）— 文字加运动学约束生成三维人体动作。**本路线的主力**
- **Mixamo** — 已否：接触类姿势没有，日常社交动作太稀
- **InterGen / InterHuman**（2023）— 双人文字生成模型和数据集，数据限科研用途
- **Inter-X** — 双人互动数据集
- **Interact2Ar**（CVPR 2026）— 双人全身（含手部）生成，代码没有明确放出
- **Text2Interact**（ICLR 2026）— 双人生成，代码称将会发布
