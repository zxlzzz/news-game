> superseded by design route 9.6.md 与 design_route_npc_motion_supply.md

# 动作供给方案 v2

状态：草案，待拷打
基线：`claude/velocity-unification-v1-946h9l` @ `07aa9f0`
作者：Claude（架构分析），最终决策权在 Hsinlung
前身：`design_motion_supply.md`（v1）。v2 不是增补，**v1 的核心分叉判断已作废**，见第 1 节。

---

## 0. 这份文档解决什么

目标仍然是那一句：

> 目标是几百个动作，当前单个动作的成本是半小时起、且会反复微调。这个量级手工产不出来。

但 v2 的答案和 v1 不同。v1 认为出路是"换一个更好的动作来源"（动捕库 / 三维骨架），
所以整篇在论证外部数据怎么进来。v2 原来的结论是：

> ~~**动作数量不是主要瓶颈，动作的"每人不同"才是。**~~
> ~~一条 clip 加上运行时的参数化变异，能顶掉几十条手工 clip；~~
> ~~而外部动捕能覆盖的那一部分（单人环境动作）恰好是你已经有的那一部分。~~

**⚠️ 重心校正（2026-08-13，与 Hsinlung 讨论后）：上面这条判断方向反了，现予降级。**

- 真实优先级是 **广度优先**。要的是大量**不同含义**的动作；而且即使把近亲（正坐/瘫坐/
  侧坐）用参数压成一族，剩下的仍有**上百种**——因为目标是模拟整个社会（街区→学校→
  饭店→…），广度不可约地大。
- "每人不同"（个性）依然要，但排**第二位**，而且它是"种类**内**"的乘法，**造不出新含义**：
  一条挥手 clip 变异出 200 个人的不同幅度，它永远还是挥手。所以第 5 节解的是次要问题，
  不是主瓶颈。

**这份文档真正要回答的问题**（一句话）：

> 在"每个动作都是手工一次性资产"这个产能天花板下，建一套**框架**，让"新增一种动作"
> 和"新增一整个场景"的边际成本低到能规模化；先用它把**街区**做完（当前主线）。

**问题的形状是一个成本方程**，框架就是"该砍哪一项"的下注：

```
总成本 ≈ (不可约的动作种类数) × (每种的作画成本)   ← 主线，v2 基本空着
        + 参数族的搭建成本                          ← 第 6 节，实为压缩引擎，应升为支柱
        + 每人变异的搭建成本                        ← 第 5 节，第二位
```

v1 押"进货"（外部动捕/三维），v2 押"一货多用"（变异）。**两者都只碰了方程的后两项；
第一项——"上百种不可约姿势，每种怎么便宜地产出"——至今没人正面回答，那才是主线。**
外部动捕（第 4 节）仍是这一项的候选来源之一，不是被否掉，是被 v2 错判成了支线。

---

## 0.5 本轮讨论确立的结构性判断

评估 v2 时和 Hsinlung 一起定的，作为后续 v3 框架设计的地基：

1. **两个正交轴：姿势 × 社交行为。** 一个动作 = 姿势基座（站/坐/蹲/走/靠/躺）× 社交那
   一层（说/交易/接触…）。"坐着说"和"站着说"不是两个动作，是"说"这层扣在两个基座
   上。混着数是 v2 把种类数估爆的原因之一。对应代码里 ModifierLayer 叠在 BaseStateMachine
   上的既有结构，不是硬套的抽象。
2. **参数族是压缩广度的主手段，已被两个例子印证**：正坐/瘫坐/侧坐 = 一个"坐"+躯干夹角；
   走近扔/远处扔 = 一个"扔"+夹角；和不同椅子交互 = 一个"坐"+定位参数。这正是第 6 节
   挂点+IK 干的事——**所以第 6 节不是附录，是广度压缩的发动机，应升为支柱。**
3. **人型（老人/小孩）不乘清单。** 老人走 vs 年轻人走 = 同一个"走"+每人变异（第 5 节），
   折进变异层，不新增种类。只有某类人**专属**的动作（小孩蹦跳、老人拄拐）才是新种类。
4. **情侣互动不是新类别，是关系标签**，骑在交谈/交易/接触上（站更近 + 接触更持续）。
   但它绝大多数落在"持续接触"（第 3 节 D 类），是唯一压不动、必须手工双人摆的角落——
   量大 = 往最贵的地方投料，成本形状要单独记。
5. **下一步不是继续论证，是盘点。** 已建 `action_inventory.md`：按社交语义把街区要的动作倒出来，
   之后横切（运动基元 / 是否需场景锚点 / 能否 overlay），数压缩族个数。**族数 ÷ 种类数
   就是"参数化压缩这条路成不成立"的答案**——可证伪，不靠嘴辩。

---

## 1. 对 v1 的三条硬性纠正

### 1.1 "骨长被真的校验" —— 错，作废

v1 第 1.5 节写着骨长逐帧被 `validate.mjs` 校验，并由此推出
"任何外部动作数据进来都必须先做骨长归一化"。

实读 `sth/tools/validate.mjs:270`：

```js
// 11. Bone length validation (non-human/non-child; human clips use free-stretch intentionally)
if (!isRoleGrouped && skelName !== 'human' && skelName !== 'child' && ...)
```

**human 和 child 被显式排除，注释写明是故意的 free-stretch。**而且这条走 `W()`
警告通道，退出码只看 `errCount`。也就是说：human clip 的骨长从来没被校验过，
以后也不打算校验。

后果不是"少一道门"，而是**骨长归一化从"必须做的转换步骤"降级为"风格选择"**。
这一条重排了整篇文档的成本表。

### 1.2 "骨架唯一缺的是第三个轴" —— 不准确

实测 64 个 human clip、3930 个骨-帧样本：

- 最大骨长偏差中位数 **9.4px**（人高 144 单位），51/64 条超 3px
- 被**压短** >10% 的样本占 **16.9%**，被拉长 >10% 的只占 **2.7%**（压短是拉长的 6 倍）
- 最极端 `squat` 的 `body→neck` 从 50 压到 **15**（比值 0.30，等价于躯干前倾 73°）
- `hand_stand_up` 多根骨压到 0.17~0.20

压短远多于拉长，这不是卡通挤压，是**手工画的透视缩短**。

**所以骨架不是"缺 z"，是"已经在存 z，只是用压短骨头这种有损方式存的"。**
这是支持骨架加 z 最强的一条论据（不是引入新概念，是把已经在做的事显式化），
也同时说明二维方案并不是"做不到深度"，而是"深度已经编码在长度里"。

本轮的实证：`assets/animations/overlay/use_trash.json` 里"向前抛"这个动作，
x 几乎没动（1→2），前臂长度从 25 → 19.65 → 15.52 → **12.65**（最高点只剩 51%）。
**前后方向完全存在于骨长里。**

### 1.3 "三套骨架" —— 实际是两套

`child` 的 `defaultPose` 和 `joints` 与 `human` **逐字节相同**，只差
`headRadius` 14/12 和 `scale` 0.59/0.85。所以 `child` 不是骨架，是两个参数。

这反过来证明**体型差异本来就是参数化的**。`Npc.js:365`
（`depthGray(this.y,…)` → NPC 颜色是纯 y 函数，四个 profile 长得一样）
的正解不是复制骨架，是加 scale / headRadius / 逐骨长度倍率。见第 5 节。

另：`unit_height` 全库零消费（grep `js/ sth/ scripts/` 无命中），死字段；
child 的 100 和它自己 defaultPose 的 144 还互相矛盾。

---

## 2. 已验证事实（HEAD `07aa9f0` 实读）

### 2.1 库存

`assets/manifest.json` 共 **67** 条：cycle 25 / overlay 33 / transition 9。
`facing`：`front` **10** 条，缺省（侧视）57 条。**没有背面。**
左右靠 `StickRenderer` 的 `joint[0] * scale * dir` 镜像 → 现状三个朝向，不是四个。

### 2.2 骨架

human 11 关节：`body neck head l_elbow r_elbow l_hand r_hand l_knee r_knee l_foot r_foot`。
手臂挂 `neck`，腿挂 `body`。**没有肩、没有髋、没有踝、没有脚趾。**
骨长：neck 50 / head 25 / elbow 29.8 / hand 25 / knee 35.2 / foot 35.1。
`defaultPose` 与 `joints.len` 完全自洽（实测 neck→r_elbow = 29.83，r_elbow→r_hand = 25.00）。

### 2.3 坐标语义（三处一致）

`abs[j] = defaultPose[j] + keyframe_delta[j]`（`ClipLibrary.js:7` 注释、
`PoseCacheBuilder.js` 的 `abs()`、`Npc.js:167` 的 overlay 重锚，三处口径一致）。
y 负值向上，`y=0` = 地面接触线。

overlay 的 delta 是相对 defaultPose 记录的，运行时由 `Npc._buildJointOverrides`
按 `NECK_ANCHORED` / `BODY_ANCHORED` 重锚到当前帧的链根，所以手臂在坐/蹲/走时
不会脱开身体。

### 2.4 已存在但零消费 / 已知缺陷

| 位置 | 内容 |
|---|---|
| `globalBend` | StickRenderer 支持骨骼弯曲，零个 clip 使用 |
| `unit_height` | 全库零消费 |
| `Motor.js:336` | `wantFront = Math.abs(accDy) > Math.abs(accDx)` **不看符号** → 远离镜头和走向镜头都放 `walk_front`，背对镜头走的人是脸朝你的 |
| `ClipLibrary.js:191` | `v.length !== 2` 是加 z 的唯一卡口，三元 delta 会被静默丢弃 |
| `validate.mjs:66` | `VALID_FACINGS = {side, front}`，要加 `back` |
| StickRenderer `BONES` | 所有骨线同色同宽、绘制顺序写死 → 手臂交叠永远分不清前后 |
| `new_assets/stumble.json` | JSON 解析失败，未注册 |
| `stand_front` / `idle_front` / `squat_front` | 零消费（speedK:0 静态态，`mot.vel` 不设，机制不到达） |

---

## 3. 动作分类：按接触关系分，不按动作种类分

v1 按"环境动作 / 接触姿势 / 道具对接"分三层。v2 保留这个骨架，
但把判据换成**是否有持续的身体接触**，因为这才是决定供给方式的那个变量。

| 类 | 判据 | 例子 | 供给方式 |
|---|---|---|---|
| A. 单人无接触 | 只有一个人，不碰任何东西 | 走 站 等 坐 蹲 刷手机 遛狗 撑伞 跳舞 | 动捕可用；**但更重要的是运行时变异（第 5 节）** |
| B. 共享物体，无接触 | 两个以上的人，但通过一个物体协调 | 传球 射门 下棋落子 递东西（松手后） | 单人 clip + 物体当同步器。**不需要双人 clip** |
| C. 瞬时接触 | 接触发生在几帧内，看完就过 | 出拳 受击 推 踉跄 击掌 | 单人 clip + 代码调度。**不需要双人 clip**，理由见 4.2 |
| D. 持续接触 | 接触要在画面上停留数秒 | 搂肩 对坐下棋 扶人 | 双人 clip（`participants` 格式已存在），手工摆 |
| E. 道具对接 | 手必须落在场景里某个具体位置 | 开门 按按钮 抓栏杆 | **运行时 IK**，不是 clip 问题（第 6 节） |

**只有 D 需要手工双人摆位。**这一类本来就少，而且 `arm_around_shoulder.json`
已经在做了，不是新增成本。

---

## 4. 外部动捕的实际定位

### 4.1 Mixamo：能用，但填不了主要缺口

用户实操评估后的结论（v2 采纳）：Mixamo 的语汇偏战斗和舞蹈，
日常单人动作覆盖有限，而那部分恰好是已经有的。

三条反对理由里只有一条成立：

- **"帧率太高，导进来动作太多"** —— 不成立。帧率是一次采样的事；
  若走 three.js 当解算器那条路（`AnimationMixer` 是连续时间的），
  帧率这个概念根本不在场。**不要拿这条当决策依据。**
- **"还是需要我实操"** —— 成立，且是决定性的。
- **舞蹈是 Mixamo 的最优情况**：单人、循环、不碰道具、不碰别人，采样进来直接用。
  而且快闪本身是个好新闻事件（一群人突然同步跳舞，玩家会举相机）。

### 4.2 "火柴人对打系统"不存在，而且不需要

搜索结论：可复用的不是代码库。存在的东西分三类，都不适用——
视频/spritesheet（拿不到关节数据，与已否决的美术资源包同一理由）、
人形骨架动画（与 Mixamo 同格式，换来源无优势）、
active ragdoll（物理解算，要常驻物理引擎，输出不是 clip）。

业界的做法是**一条动画装两个角色**（Maya 叫 sync bone，UE 叫 paired animation），
所有参与者相对同一个公共点定义，运行时找一个满足条件的位置放这个公共点。
**这条对本项目不适用**：那套约束来自蒙皮模型贴近了看会穿模。
本项目的人在屏幕上 **47.6px** 高（144 × 0.85 = 122.4 单位，经 `PX_PER_UNIT`），
线宽 3px，拳头就是一个点。**一拳打偏几个像素看不见。**

所以 C 类全部走单人 clip。让人读出"两个人在打"的只有三件事：
面对面、一方出手后另一方隔几帧后仰、距离一收一放。**三件都是调度，不是动画数据。**

**够不着的问题有现成口子**：human 是 free-stretch（1.1 节），
出拳那一帧直接把手臂关节拉向对方，没有门会报错。
三维里够不着就是够不着——这是二维在这一类上反而更宽松的地方。

### 4.3 三维骨架：不再是分叉

v1 把"烘焙成二维 delta"和"骨架升三维"当成必须拍板的核心分叉，
理由之一是三维才能拿到运行时 IK。**这条理由本轮被推翻**（第 6 节）。

剩下的差别只有一条：**三维的透视缩短自动出来，二维要给一个压缩系数。**
而压缩系数正是现有 clip 已经在手工给的东西（1.2 节）。

结论：三维不是前置条件，是一个可以随时做也可以永远不做的优化。
`ClipLibrary.js:191` 的 `v.length !== 2` 是唯一卡口，一行。
真要做的时候再做，不阻塞任何别的事。

若要做，推荐 **three.js 只当解算器不当渲染器**：
加载 FBX/glTF → `AnimationMixer.update(dt)` → 只读 11 根骨头的 `getWorldPosition()`
→ 交给 StickRenderer 画线。全程不创建 WebGL 上下文、不调 `renderer.render()`。
更省的做法是加载时采样成 N 帧，运行时连 three.js 都不在场——
**因为画的是线不是蒙皮，pose 之间可以直接在位置空间线性插值，不会蒙皮塌陷。**

---

## 5. 运行时变异：个性轴（第二位；非主瓶颈，见 §0 重心校正）

> 本节机制成立、仍要做，但它解的是"每人不同"这个**第二位**问题——种类内的乘法，
> 造不出新含义。原标题"本文档的主要主张"已按 §0 校正撤下。

### 5.1 为什么不烘进 JSON

烘进 JSON 的随机数在出厂时就定死了，全场每个人拿到同一个"随机"值。
要的是人跟人不同，所以随机必须按 NPC 生成 → 必须在运行时。
改 67 个文件是把问题改到错的地方。

### 5.2 机制已经半存在

`ClipLibrary.js:174` 的 `variant_of` + `amp` 就是"同一份 keyframes 乘一个系数"，
`variant/older/*` 走的正是这条路。它唯一的毛病是 `this._resolved[id]` 按 clip id
缓存 → 一个 clip 全世界一个 amp。把这个乘法挪到绘制时就变成每人一个。

### 5.3 闸口只有一个

`StickRenderer._drawHuman`：

```js
const coord = (j) => (ov && ov[j]) ? ov[j] : frame[j];
```

所有人形 NPC、所有 clip、所有帧、连 overlay 覆盖都从这一个函数出去。
在这里插一个按 NPC 的变换，**67 条 clip 一次全覆盖**。

### 5.4 旋钮清单

| 旋钮 | 做法 | 注意 |
|---|---|---|
| 动作幅度 / 手的位置 | **末端目标点 + 余弦定理重建，见 5.5** | 逐关节乘系数的写法已作废，见下 |
| 走路步幅 | 脚的目标点 x 行程乘 k **且** `Npc.js:324` 的 `travelUnit = anim.groundTravel * this.scale` 同乘 k | **必须同乘**，否则脚在地上滑。这是"两个文件而不是一个"的全部原因 |
| 身高 | `Npc.js:96` 的 `this.scale` 从固定配置改为按 NPC 抽 | |
| 头身比 | `headRadius` 从按骨架查表改为按 NPC | |
| 步频 / 相位偏移 | 每人一个 `timeScale` + 初始 `phase` 偏移 | `phase` 已经是 `[0,1)` 且跨 clip 不清零，天然支持 |

后两项直接解掉 `Npc.js:365`"四个 profile 长得一样"。

**已作废的写法（Claude 提过，自己推翻）**：`dp[j] + (frame[j] − dp[j]) × k`
逐关节各自往自己的静止位置收。幅度确实变小，但肘和手收的方向不同，**中间那根骨头
被拉短或拉长**。这正是 CLAUDE.md 那条铁律禁掉的形状（禁止任何工具直出关节坐标，必破骨长）。

**同时作废的替代方案**：转成角度、只缩角度、再正向运动学重建。骨长确实守住了，
但转了小腿角度脚就离地，事后必须重新归零；而且步幅与角度是 sin 关系，
`groundTravel` 的系数对不上。**5.5 的做法两个毛病都没有。**

### 5.5 通法：末端目标点 + 余弦定理（本节是 v2 的技术核心）

**骨架拓扑实读（`assets/skeleton.json`）**：

```
body (root) ├─ neck ─┬─ head
            │        ├─ l_elbow ─ l_hand
            │        └─ r_elbow ─ r_hand
            ├─ l_knee ─ l_foot
            └─ r_knee ─ r_foot
```

**从任何一个分叉点往下数，最长就是两根骨头。全身没有一条三骨链。**
两臂（neck→肘→手）、两腿（body→膝→脚）四条都是标准两骨链；
body→neck 和 neck→head 各是单根骨头，直接给角度，没有肘要解。

因此一帧姿势可以完整改写成：

> body 位置 + neck 一个角 + head 一个角 + **四个末端目标点**
> （两手相对 neck、两脚相对 body）+ 四个弯曲方向 + 每链两个骨长

**这是无损换算，不是近似。**换回去就是余弦定理。
**所以余弦定理不是"我为 use_trash 找的特例"，它是这套骨架的完整通法。**

变异 = 只挪末端目标点，肘和膝由余弦定理重算。**骨长按构造精确保持。**

**它比另外两条路强在两处，都是会真的碰上的**：

1. **脚不会离地。**把脚的目标 y 设成 0，它就在地上，**没有归零这一步**
   （角度 FK 必须事后归零）。
2. **`groundTravel` 变成线性。**直接缩放脚的水平行程，步幅乘 k 就是乘 k，
   `travelUnit` 同乘 k 精确对上（角度空间是 sin 关系，对不上）。

**它不白给的三件事**：

- **超出两骨之和无解。**改 `use_trash` 时实际撞上了。要么拉长骨头去够（本轮选的），
  要么把目标点钳回来。**这是唯一需要人拍板的地方，必须写成一条明文政策，不能每处各判一次。**
- **链快伸直时肘的位置数值不稳，会左右翻面。**走路的膝盖在支撑帧就接近伸直。
  解法：带着作者原来的弯曲方向走，不要每帧重算。
- **它只负责重建，不负责决定改什么。**目标点往哪挪、挪多少是设计，不是算术。

**顺带解掉一个 5.3 的问题**：`coord()` 是逐关节的闸口，而重建必须整条链一起算。
所以变换要插在 `coord()` 的**上游**——一个"整副 pose 进、整副 pose 出"的函数，
`coord()` 本身不动。

### 5.6 改动落点与门

- `js/core/StickRenderer.js` —— 在 `coord()` 上游插整副 pose 的重建；
  `draw()` 多收一个变异参数（371 行已经在传 scale/direction，顺路）
- `js/npc/Npc.js` —— 出生时按种子算一次变异系数；324 行乘上去

**门天然绿**：`check-invariants` Rule 4（`speedK>0 → |meanX| ≤ 4`）和
Rule 16（groundTravel 腿贡献 ±20%）都是静态读 JSON 的，不改 JSON 就不受影响。
这是选运行时的第三个理由。

---

## 6. 道具对接：IK 不需要三维

> **重定位（§0 校正）：本节是广度压缩的发动机，不是附录。** "一种坐 + 定位参数吃掉所有
> 椅子"、"一种伸手 + 挂点吃掉门/按钮/栏杆/垃圾桶"——参数族靠的就是这里的挂点 + IK。
> 应作为 v3 框架的支柱之一，与第 5 节（个性轴）并列。

### 6.1 本轮已验证（不是主张，是跑过的）

用余弦定理写的两骨 IK，喂进 `use_trash.json` 作者原来 11 帧的手的位置 + 每帧实际骨长，
反算肘的位置 —— **11 帧全部误差 0.00000，逐帧与原文件整数相同**。
坐标约定（y 负向上 / 肘在 neck→hand 连线哪一侧 / delta 相对 defaultPose）
经 `Npc.js:165` 重锚逻辑确认。

在此基础上做了一次真实改动（手的位置右移、抛动幅度加大）：
保留作者的**肘夹角**和**上臂:前臂比例**，两根骨自由伸缩去够新目标点。
这样作者的透视缩短逻辑跟着新位置走，不会出现"手伸到右边了但前臂还是个短桩"。
六个静态门全绿，`use_trash` 本身零 warning 零 error。

**两个旋钮**：向内 travel × 0.52（最伸出帧手的 x 从 1 → 13，静止位 26）；
抛动幅度 × 1.7（上挑 10 → 17 单位）。改这两个数肘会自己重算——这就是 IK 的意义。

### 6.2 插口已经存在

`npc.overlayPose` 的 jointOverrides 当初就是为"代码控制关节、不新建动画文件"做的。
IK 算出的肘和手直接写进去。

### 6.3 真正缺的是数据，不是逻辑

`scene.json` 里道具只有位置和尺寸，**没有挂点声明**
（"门把在高 42、水平偏移 −18"这种）。这是唯一的新增工作，而且是数据不是逻辑。

收益形状：**挂点一旦有了，一条"伸手"clip 加 IK 覆盖手臂，
就能对上任意高度的按钮、任意位置的门把、任意高度的栏杆。**
一条 clip 覆盖一整类 —— 与第 5 节同一个方向：把资产换成参数。

---

## 7. 待测（必须先做，因为会锁死后面所有决定）

### 7.1 骨架够不够用

关节数一旦定下，Mixamo 映射表 / stick-puppet / pose 缓存全跟着定。
**这件事能测不能猜**：拿最难的十个姿势
（正坐 / 瘫坐 / 背靠椅子 / 斜着坐 / 坐地上 / 搂肩 / 递东西 / 推人 / 开门 / 弯腰捡）
在 stick-puppet 里**只摆一帧**，看哪些摆不出来、卡在哪个关节。

Claude 的预测（待验证）：集中在**躯干**（`body→neck` 一根 50 的硬杆，没有腰）
和**脚**（没有踝，脚是一根线段，坐着时脚的角度做不出来）。

**若要加关节，唯一能完全向后兼容的加法**：加 `chest`（或 `spine`），
默认值取 `body`/`neck` 中点 → 两段共线，老 clip 渲染结果逐像素不变，只有新 clip 用它。

**不该加**：肩、髋、脚趾、手指。47.6px 高 + 3px 线宽画不出来；
而且每加一个关节，摆姿势的成本线性上涨，那才是真实成本。

### 7.2 两个五分钟的止损点（都不写代码）

1. Mixamo 搜十个词：sitting / waiting / talking / texting / leaning / smoking /
   carrying / looking around / standing idle / crossing arms，看日常覆盖率
2. CC0 低模站（Kenney / Quaternius / Poly Pizza / Sketchfab CC0）搜
   city / street / urban kit，看能否凑出三十件街区填充物

---

## 8. 优先级：这份文档排在画面后面

画面四步会推翻这里的部分参数（尤其 47.6px 这个尺度前提），顺序不能乱：

1. 实机 `resolution` 修掉（一行；导出图比实机好看纯粹是 1.86 倍超采样）
2. 尺度定标（现在一层楼只有 0.65 个人高，道具和建筑普遍比人小 2~3 倍）
3. SHEAR / TILT 挂滑条亲眼调（这两个数从没调过，`Projection.js` 原注释就写着"暂定值"）
4. `drawObliqueBox` 加旋转 + 多边形足迹
5. —— 然后才是本文档：第 7.1 骨架测试 → 第 5 节运行时变异 → 第 6 节挂点 + IK

第 5 节是唯一可以和画面并行的一块（它不碰投影，也不碰 JSON）。

---

## 8.5 关于"只改几个关键姿势"这条路（实读结论）

作画时 `walk` 只摆了五个姿势，其余是手工补出来的。但**落到文件里补全的痕迹不存在**：
`walk.json` 是 **20 个完整关键帧，每帧 11 个关节全部写死**，
且播放端无插值（`StickRenderer` 逐帧跳）。
所以"只改两个抬脚姿势、其余自动补全"在当前数据结构上没有落点——补全这一步不在运行时。

**但半周期镜像是真的，量到了**：第 i 帧与第 i+10 帧左右镜像后，
**最大关节差只有 2~9px**（人高 144 单位，典型 4~5px）。
walk 实际是 10 个姿势 + 镜像，不是 20 个独立姿势。

**结论：这条路不必走。**5.5 的通法里"走路步幅随机"就是把每帧两只脚的
x 行程乘 k，20 帧一起算，一条 clip 都不用重画。
分类特判只在通法解不了的地方才需要，而目前没找到通法解不了的地方。

---

## 9. 仍然待拷打的地方

- 第 5.5 那条"超出两骨之和怎么办"的政策没定（拉长 vs 钳回）。
  这是通法里唯一需要人拍板的分岔，定不下来每处会各判一次。
- 第 5 节的变异系数取值范围完全没定。需要挂滑条亲眼看，不是算出来的。
- 第 3 节 C 类"三件都是调度"里，"距离一收一放"要靠 DuetStager 的会合层，
  而那一层至今没实现（现在拿 `REACH_SLACK=2.5` 顶着）。C 类的真实成本在这里，
  不在动画数据。这一条 v2 没有给方案。
- 第 6.3 挂点声明的格式没定。它必须和道具的 slots / affordance 对齐
  （摊位 / 棋桌 / 公交站台已经有交互槽位），不能各搞一套。
- 道具供给本身是同一个供给问题的另一半：22 个 draw 文件共 2295 行，
  去掉 building(367) / vehicle(468) 后平均一个道具 45 行手写 JS。
  200 个道具 = 9000 行。v2 没有覆盖这块（属于画面第 5 步）。
- 舞蹈作为新闻事件的触发条件、`stumble.json` 修不修、背面 clip 补不补，都未决。