# 场景规格（2026-09-23）

目标：改文件就能改场景，以便快速建新场景。
原则：**文件只记推不出来的，其余由代码推导。**
做法：走 Godot 自己的方式，场景本身就是 `.tscn` / `.tres` 文本文件，编辑器里可以拖着摆，也可以直接改文本。

模型来源：他向 ChatGPT 提要求，ChatGPT 自己找素材、自己搭建。模型要符合根目录《建模规范与参数.md》：单位米、原点在底面接地点、正面朝 +Z、材质名就是色槽名、只允许等比缩放。

---

## 1. 一个场景 = 一个文件夹

放在 Godot 工程 `godot/` 里：`scenes/<场景名>/`。

| 文件 | 写什么 | 为什么推不出来 |
|---|---|---|
| `level.tscn` | 地面带节点 + 各物件实例（类型、位置、朝向）；引用下面三份 | 摆什么、摆在哪是这个场景的设计 |
| `palette.tres` | 配色：色槽 → 颜色；明暗系数（`mid_mul` / `dark_mul` / `stroke_mid_mul` / `stroke_dark_mul`）；线色；角落色晕；背景色 | 配色是选出来的。现有两套 params 里，这几项正好是随配色一起变的 |
| `view.tscn` | 相机节点 + 方向光节点 | 构图和光的方向是这个场景的选择 |
| `population.tres` | 各类路人的数量（普通路人多少、有没有跑步的、遛狗的） | 场景设定 |

配色和视角可以不放在自己文件夹里，而是指向一份共用的：几个街景用同一套配色，就引用同一个文件。

布局不拆：物件是摆在某条地面带上的，拆开后改了带宽，另一个文件里的物件会悄悄错位。

### 地面带

`level.tscn` 里放一个编辑器里也会运行的脚本节点（`@tool`）。它的属性是一串带，每条带写宽度和材质名（例如 road 7 米、sidewalk 3 米、grass），外加整条的长度。这个节点生成地面网格，改了属性编辑器里立刻重建。

露台那种不是一条条带子的场景，直接放一个整模型的实例，不用这个节点。

---

## 2. 全局共用（不属于任何场景）

| 文件 | 写什么 |
|---|---|
| `types/<类型>.tscn` | 物件类型库。每个类型从 glb 继承：模型、缩放（等比，一个数）、交互点（坐的位置、门、摊位后面站人的位置，用 Marker3D 标）、标签（节点分组 groups） |
| 色槽定义 | 每个色槽的标记：`flat` / `no_lines` / `no_shadow` / `hide`。这些是"这类表面怎么画"，跟场景无关 |
| 第三方材质映射 | 第三方素材的材质名 → 色槽（现在的 `slot_map`，支持通配符）。按素材包走，不按场景走 |
| 画风参数 | `band_ndl`、排线周期、三个覆盖率、线宽、线的深度偏移、`crease_deg`、`light_scale`。现有两套 params 里这些值相同 |

---

## 3. 推出来的，不写

| 东西 | 从哪推 |
|---|---|
| 物件尺寸 | 模型包围盒 |
| 能走哪、走起来代价多大 | 地面带的材质名。同一个名字既定颜色，也定走动代价（road 高、sidewalk 低、建筑不可走）。~~用 NavigationRegion3D，每种代价一个区域~~ 实现改用网格寻路（`godot/core/walk_grid.gd`，代价表 `godot/core/walk_costs.json`）：分区域烘焙会让代价不同的相邻区域连不上，原因见该文件开头（2026-09-25） |
| 斑马线 | 斑马线是一个物件类型。地面节点生成可走区域时，按场景里斑马线实例的位置把车道那段切出来，按人行道代价算 |
| 出口 | 可走区域碰到场景边缘的地方 + 建筑类型里标的门 |
| 生成点 | 出口 + 门 |
| 坐哪、在哪买东西、从哪进门、标签 | 物件类型库 |
| 固定岗位的人（摊贩、下棋的） | 跟着物件：摆了摊位就有摊贩，摆了棋桌就有下棋的 |
| 跑步、遛狗的路线 | 在可走区域上现算 |
| 同类物件的大小差异（树、灌木、石头） | 代码按实例位置算一个固定的小幅随机缩放，同一位置每次结果相同 |

---

## 4. 规矩

- 场景里的实例**不改缩放**，缩放只在类型里设。要大小两号，就在类型库里登记两个类型，指向同一个模型、各带一个缩放值。
- 场景文件不写尺寸。
- 不加"想到啥就加啥"的字段：新加一项之前，先确认它推不出来。

---

## 5. 和现有工程的关系

原来的 `sth/godot-npc/` 用 `params/*.json` 加运行时 GLTFDocument 加载 glb，是之前在容器里没有编辑器时的做法。2026-09-23 起新工程是 `godot/`，改走编辑器导入 glb，场景写成 `.tscn`；旧工程已删除。

现有 `params/*.json` 的字段拆到哪：

| 现有字段 | 去处 |
|---|---|
| `models` | `level.tscn` 的实例（glb 先做成 `types/` 里的类型） |
| `palette` 的颜色 | `palette.tres` |
| `palette` 的 `flat` / `no_lines` / `no_shadow` / `hide` | 全局色槽定义 |
| `slot_map` | 全局第三方材质映射 |
| `fill` 的四个乘数、`line.line_color`、`grade`、`background` | `palette.tres` |
| `fill` 其余、`line` 其余、`crease_deg` | 全局画风参数 |
| `camera`、`light` | `view.tscn` |
| `resolution` | 不属于场景，出图 / 项目设置 |
| `sticks` | 不要。人由 `population.tres` 和物件推出来 |