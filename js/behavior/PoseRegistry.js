/**
 * PoseRegistry — 兼容入口（数据已迁移至 assets/animations/ JSON）
 *
 * 运行时数据由 poseCache 注入（StreetScene.create → BehaviorManager → initPoseCache）。
 * 本文件仅为 sth/anim-preview 工具保留 import 兼容性。
 */

export const HELD_POSES      = {};
export const TRAIT_PROPS      = {};
export const GESTURE_CLIPS    = {};
export const LOITER_POSES     = {};
export const SUB_EVENT_POSES  = {};
