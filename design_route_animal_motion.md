> **状态：草案 v3（2026-09-22）。** 两条路都做了 3D 原型，还没拍板用哪条当主力。
> 原型在 `sth/animal-study/`（说明见其 README），预览页 `D:\mocap-trial\preview\dog3d.html`。
> 本文取代 `anim.md` 里"只走视频动捕"的结论；`anim.md` 的 rig 实测部分仍有效。

# 动物动作 — 路线（草案）

## 0. 背景

- NPC 是火柴人，运行端在 Godot（`godot/`）；`js/` 已冻结。
- 人的管线：每帧 3D 源关节 → 共用纯映射函数 + 参数 JSON → 要画的 3D 点 → 按当前朝向转、投影、按深度画。
- 动物要求（Hsinlung，2026-09-22）：**画成纯黑剪影**；和人一样用 3D 坐标实时转换，**能看出转向的 3D 感**。

## 1. 已经试过的

### 1.1 视频动捕（MoCapAnything V2，官方 HF Space）

- 用 `mocap_space.py` 直接调官方网页版（Gradio 队列接口），不用本机装环境。
- 参考骨架用的是 demo 自带的 Dog（Truebones，53 关节）。**结果只能本地预览，不进仓库。**
- 跑过两段：官方样例侧拍小跑（背景已抠）、维基共享资源的旧金山街拍黑色长毛狗（CC BY-SA 3.0）。
- 结论：
  - 姿势能读出来（腿交替、尾巴、低头），难的街拍素材也行。
  - `*_pose_pred.npy` 骨长不恒定（同一根骨头长度浮动 50%–90%），**要用 BVH 做正向运动学**才得到刚性骨架。
  - 输出是**原地踏步**（Hips 水平固定在原点），前进量要从着地脚反推。
  - 画成纯黑剪影后问题更明显：**脚会滑、常悬空**，着地判断不准（按高度判只有 4%–26% 帧着地）。
  - 放进游戏的用法（沿弯曲路线播放）时：速度固定，不会减速、不会停，转弯只能硬拧，脚滑更重。
- 限制：免费 ZeroGPU 额度约 5 分钟/天，每次预留 180 秒 → **一天只能跑一段**。

### 1.2 程序生成步态（`dog3d.template.html` 内）

- 每只脚是地面上的世界坐标：着地时钉死，抬脚时按身体速度预测落点、抛物线过去。
- 走（侧对步序 LH→LF→RH→RF，着地占比 0.64）与小跑（对角步，0.42），按速度自动切换，相位平滑过渡。
- 腿三段：两骨 IK（膝/肘弯向取固定方向）+ 末端固定方向的一节（掌/跖），抬脚时末端内收。
- 身体：肩带跟着航向走，髋带被拖着跟（弯道时身体自然弯）；头在转弯时先转向、停下时左右看；尾巴左右摆。
- 路线跟随：前视点转向、弯道减速、停靠点停 3 秒；停下时如果脚不在身体下方会补一两步再站定。
- 输出每帧就是 3D 点，和人的"3D 点 → 转向 → 投影 → 画"完全同一套。
- 结论：脚不滑，速度和转向都跟得上，纯黑剪影下观感干净。

## 2. 建议（待拍板）

**移动（走、小跑、转弯、起步、停下）用程序生成；原地动作（坐、趴、嗅地、抖毛）用少量手摆的 3D 关键姿势加过渡；视频动捕降为参考/补充。**

程序生成还要补的：

1. 原地动作：做成关键姿势（3D，和程序输出同格式），加"走 → 坐下 / 趴下 → 起身"的过渡。
2. 起步、原地掉头的专门规则（现在只有"停下补步"）。
3. 避免机械感：步长、节奏、头尾动作加随机微差。
4. 地面高度（台阶、路沿）。
5. 比例参数化：像人的 `skeleton_tuner.html` 那样做调参页，Hsinlung 调出狗 / 猫各一份。
6. 移植进 Godot：步态写成纯函数（输入：上一帧状态 + 目标速度/航向 + 参数；输出：3D 点 + 新状态），`.mjs` 与 `.gd` 各一份，对拍。
7. 鸽子：两足 + 翼，另写一套；飞行单独立题。

## 3. 需要 Hsinlung 决定

1. 按 §2 定下主路线吗？
2. 动捕还要不要继续多跑？免费额度一天一段。要多跑有三个办法：
   本机装环境（约 10GB，放 D 盘，8GB 显存够不够未知）、实验室 3060 12GB（要挂 VPN）、
   或 HF PRO（每月约 9 美元，40 分钟/天）。
3. 程序生成原型的外观（比例、粗细、尾巴、耳朵）先由 Claude 定，还是先做调参页给你调？

## 4. 许可

- 程序生成：无第三方素材，可以进仓库。
- 视频动捕：视频按"仅研究、记来源"；现在的输出依赖 Truebones 参考骨架，不进仓库。
  要进仓库得换成我们自己的 rig（Quaternius CC0，需在 Blender 里清 IK 骨、补爪子骨、导 FBX、跑官方自定义 rig 预处理）。
